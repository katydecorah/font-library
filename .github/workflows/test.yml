name: Test

on:
  push:
    paths-ignore:
      - "families.json"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
      - run: npm ci
      - run: npm run build
      - run: npm test
        env:
          CI: true
