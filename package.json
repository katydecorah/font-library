{
  "name": "font-library",
  "version": "0.0.0",
  "description": "An open source project to tag and organize Google Fonts.",
  "scripts": {
    "test": "scss-lint _sass/*.scss && jsonlint families.json -q"
  },
  "dependencies": {
    "jsonlint": "^1.6.2",
    "scsslint": "0.0.3"
  }
}
