{
  "name": "font-library",
  "version": "0.0.0",
  "description": "An open source project to tag and organize Google Fonts.",
  "scripts": {
    "test": "scss-lint _sass/*.scss && jsonlint families.json -q && tape test/*.js | tap-min"
  },
  "dependencies": {
    "json-stringify-pretty-compact": "^1.0.2",
    "jsonlint": "^1.6.2",
    "scsslint": "0.0.3"
  },
  "devDependencies": {
    "js-yaml": "^2.1.3",
    "request": "^2.79.0",
    "tap-min": "^1.1.0",
    "tape": "^4.6.3"
  },
  "bin": {
    "update-library": "bin/update.js"
  }
}
